

export const temp = () => {
    return {
        type: "TEMP",
        id: 344
    }
}

export const remove = () => {
    return {
        type: "REMOVE",
        payload: 890
    }
}
export const
    setInputTitle= (title) => ({
      type: "SET_INPUT_TITLE",
      title,
    }) 
    export const
    setInputAmount= (amount) => ({
      type: "SET_INPUT_AMOUNT",
      amount,
    }) 
    export const
    setInputNote= (note) => ({
      type: "SET_INPUT_NOTE",
      note,
    }) 
    export const
    setInputDate= (date) => ({
      type: "SET_INPUT_DATE",
      date,
    }) 
    export const
    setReset= () => ({
      type: "RESET_INPUT",
      
    }) 
